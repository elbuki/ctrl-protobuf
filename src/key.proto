syntax = "proto3";

package pb;

option go_package = "./";

// Key is the representation of a key in the keyboard to standarize the codes
enum Key {
    ENTER = 0;
    ESC = 1;
    F11 = 2;
    LEFT_ARROW = 3;
    RIGHT_ARROW = 4;
    ALT_TAB = 5;
}
